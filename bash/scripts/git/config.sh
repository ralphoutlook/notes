# vim: softtabstop=2 ts=2 sw=2 et
#!/bin/bash
# list global config
git config -l --global
# list all config
git config -l
# config user.name, email and editor
git config --global user.name brainchildralph
git config --global user.email brainchildralph@gmail.com
git config --global core.editor vim

